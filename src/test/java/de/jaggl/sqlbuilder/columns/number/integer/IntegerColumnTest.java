package de.jaggl.sqlbuilder.columns.number.integer;

import de.jaggl.sqlbuilder.columns.number.NumberColumnTest;

abstract class IntegerColumnTest<C extends IntegerColumn<C>, B extends IntegerColumnBuilder<B, C>> extends NumberColumnTest<C, B, Integer>
{
    // nothing to do here so far
}
